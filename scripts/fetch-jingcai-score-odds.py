#!/usr/bin/env python3
"""DEPRECATED — use fetch-jingcai-odds.py instead.

This script only fetches poolCode=crs (波胆).
fetch-jingcai-odds.py fetches both crs (波胆) + hhad (让球胜平负) simultaneously.
"""
import sys
print("DEPRECATED: use fetch-jingcai-odds.py instead (supports both 波胆 + 让球)", file=sys.stderr)
sys.exit(1)
